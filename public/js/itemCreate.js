$(document).ready(() => {

  $("form").on('submit', (e) => {
    const title = $('#title').val()
    const desc = $('#desc').val()
    const daily_price = $('#daily_price').val()
    e.preventDefault()
})

  // const item = {
  //   title,
  //   desc,
  //   img_url,
  //   cat,
  //   owner_id,
  //   rented,
  //   daily_price
  // }

  // $.ajax(item)
})
